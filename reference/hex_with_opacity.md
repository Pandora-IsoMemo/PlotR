# Convert hex to hex with opacity

Convert hex to hex with opacity

## Usage

``` r
hex_with_opacity(hex, opacity)
```

## Arguments

- hex:

  color in hex format

- opacity:

  opacity value between 0 and 1
