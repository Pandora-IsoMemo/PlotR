# Get plotValues

Get plotValues

## Usage

``` r
fitModel(plotValues, modelParameters)
```

## Arguments

- plotValues:

  (list) list with all values required for the plot

- modelParameters:

  (list) list of settings for the model
