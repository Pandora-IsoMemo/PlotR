# Pop up a Warning if Req is missing.

Pop up a Warning if Req is missing.

## Usage

``` r
checkReq(x, label)
```

## Arguments

- x:

  reactive object to be checked with req()

- label:

  (character) Warning that pops up if x is missing
