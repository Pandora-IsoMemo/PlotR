# Server function for model settings

Backend for model settings module

## Usage

``` r
modelSettings(input, output, session, modelParameters)
```

## Arguments

- input:

  shiny input object

- output:

  shiny output object

- session:

  shiny session

- modelParameters:

  (reactive) list of settings for input fields
