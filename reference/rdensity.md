# Generate n random numbers from density() object

Generate n random numbers from density() object

## Usage

``` r
rdensity(n, x, probs, bw)
```

## Arguments

- n:

  The total random numbers to generate

- x:

  numeric vector

- probs:

  probabilities for x

- bw:

  bandwidth used in density()
