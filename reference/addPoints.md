# Add points module

UI function to define point values for a point to be added to the plot

Backend for add points module

## Usage

``` r
addPointsUI(id)

addPoints(input, output, session)
```

## Arguments

- id:

  id of module

- input:

  shiny input

- output:

  shiny output

- session:

  shiny session
